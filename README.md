<h1 align="center">
🔄 Data Pipeline для MinIO
</h1>

<h3 align="center">
⚡ Асинхронная обработка CSV-файлов с фильтрацией и загрузкой в облачное хранилище
</h3>

---

## 📋 Обзор системы

Data Pipeline представляет собой **асинхронное приложение** для автоматизированной обработки CSV-файлов, их фильтрации и загрузки в объектное хранилище **MinIO** с поддержкой логирования и архивации.

---

## 🏗️ Архитектура решения

### 📁 **Структура проекта:**

```
├── pipeline.py              # Основной пайплайн обработки
├── data_generator.py       # Генератор тестовых данных
├── config.py              # Конфигурация приложения
├── data/                  # Локальное хранение данных
│   ├── input/            # Входные файлы для обработки
│   ├── temp/             # Временные файлы
│   └── archive/          # Архив обработанных файлов
└── logs/                 # Логи работы приложения
```

---

## 🔧 Детальное описание работы пайплайна

### **1. 📥 Инициализация пайплайна**

#### **Конфигурация:**

```python
MINIO_CONFIG = {
    'endpoint_url': 'http://localhost:9000',     # URL MinIO сервера
    'access_key': 'minioadmin',                  # Ключ доступа
    'secret_key': 'minioadmin',                  # Секретный ключ
    'bucket_name': 'bucketmaks',                 # Имя бакета
    'input_folder': './data/input',              # Папка для мониторинга
    'max_concurrent_files': 5,                   # Максимум параллельных обработок
    'bucket_versioning': 'Enabled'               # Версионирование бакета
}
```

#### **Инициализация клиентов:**

- 🔄 **Синхронный клиент:** для проверки подключения и управления бакетами
- ⚡ **Асинхронный клиент:** для параллельной загрузки файлов
- 🔒 **Семафор:** ограничивает количество одновременно обрабатываемых файлов (по умолчанию: 3)

### **2. 👁️ Мониторинг папки**

#### **Механизм отслеживания:**

- Используется библиотека `watchfiles`
- **Фильтр:** отслеживаются только `.csv` файлы
- **Дебаунс:** 3000 мс для избежания ложных срабатываний
- **Шаг проверки:** 2000 мс
- **Рекурсивность:** отключена (только корневая папка)

#### **Алгоритм обнаружения:**

1. Пайплайн постоянно мониторит папку `input_folder`
2. При появлении нового файла срабатывает событие `Change.added`
3. Файл добавляется в очередь обработки
4. Запускается асинхронная задача `process_file()`

### **3. 🔄 Обработка файла**

#### **Шаг 1: Проверка стабильности файла**

```python
async def wait_for_file_stable(file_path, timeout=30):
    """Ожидает завершения записи файла"""
```

#### **Шаг 2: Чтение и фильтрация данных**

```python
def filter_data(df):
    """Фильтрует данные по условиям (возраст от 18 до 40 лет)"""
```

#### **Шаг 3: Сохранение во временный файл**

```python
async def save_temp_file(df, original_file):
    """Создает временный файл filtered_{имя}_{timestamp}.csv"""
```

#### **Шаг 4: Загрузка в MinIO**

```python
async def upload_to_minio(file_path, original_file):
    """Формирует ключ S3: processed-data/YYYY/MM/DD/filtered_имя_файла.csv"""
```

#### **Шаг 5: Архивация исходного файла**

```python
async def archive_source_file(file_path):
    """Перемещает файл в архивную папку archive/YYYY/MM/DD/"""
```

### **4. 📝 Система логирования**

#### **Двойное логирование:**

- 📄 **Локальный файл:** `logs/minio_pipeline.log`
- 💻 **Консольный вывод:** для мониторинга в реальном времени

#### **Формат логов:**

```
2024-01-15 10:30:45 - __main__ - INFO - Обнаружен новый CSV файл: data_batch_001.csv
2024-01-15 10:30:48 - __main__ - INFO - ✅ Файл обработан: processed-data/2024/01/15/filtered_data_batch_001.csv
```

#### **Периодическая загрузка логов в MinIO:**

- Логи аккумулируются локально
- Каждые 10 секунд проверяется время последней загрузки
- Если прошла 1 минута И есть новые логи → выполняется загрузка
- После загрузки локальный лог-файл очищается

### **5. 🔄 Управление версионированием**

#### **Настройка версионирования бакета:**

```python
def enable_bucket_versioning():
    """Включает версионирование для бакета"""
```

#### **Преимущества версионирования:**

- ✅ Сохраняются все версии лог-файлов
- ✅ Возможность восстановления предыдущих версий
- ✅ Аудит изменений

### **6. 🧪 Генерация тестовых данных**

#### **Особенности генератора:**

```python
def generate_large_csv(filename, rows=10000, batch_size=2000):
    """Генерирует реалистичные данные на русском языке"""
```

#### **Формат имен файлов:**

```
data_batch_001_20241218_143025.csv
├── номер пачки (001)
├── дата (20241218)
└── время (143025)
```

---

## ⚡ **Асинхронная обработка**

### **Параллельная обработка файлов:**

```python
self.semaphore = asyncio.Semaphore(max_concurrent_files)
```

- Ограничивает количество одновременно обрабатываемых файлов
- Предотвращает перегрузку системы
- Настраивается через `MAX_CONCURRENT_FILES`

### **Асинхронные операции:**

- 📤 **Загрузка в MinIO:** `aioboto3` для параллельных загрузок
- 👁️ **Мониторинг папки:** асинхронный итератор `awatch`
- 📊 **Фоновая проверка логов:** отдельная задача `background_log_checker`

---

## 🛡️ **Обработка ошибок**

### **Механизмы обработки:**

- 🔍 **Проверка подключения:** при старте проверяется доступность MinIO
- ⏳ **Ожидание стабильности файла:** избегание чтения частично записанных файлов
- 🚨 **Обработка исключений:** каждая стадия обрабатывает свои исключения
- 🧹 **Финальная очистка:** при завершении работы загружаются накопившиеся логи

### **Восстановление после ошибок:**

- 📁 Файлы остаются во временной папке при ошибке загрузки
- 💾 Исходные файлы не удаляются до успешной обработки
- 📝 Логи сохраняются даже при аварийном завершении

---

## ⚙️ **Конфигурационные параметры**

### **Основные настройки:**

| Параметр | Значение по умолчанию | Описание |
|----------|----------------------|----------|
| `MINIO_ENDPOINT` | `http://localhost:9000` | URL сервера MinIO |
| `MAX_CONCURRENT_FILES` | `5` | Максимум параллельных обработок |
| `AGE_MIN` | `18` | Минимальный возраст для фильтрации |
| `AGE_MAX` | `40` | Максимальный возраст для фильтрации |
| `BUCKET_VERSIONING` | `Enabled` | Статус версионирования бакета |

### **Структура папок:**

```
data/
├── input/      # Входные файлы (мониторится)
├── temp/       # Временные файлы (очищаются после загрузки)
└── archive/    # Архив обработанных файлов (сохраняются)
logs/
└── minio_pipeline.log  # Логи работы
```

---

## 🚀 **Запуск и использование**

### **1. Запуск пайплайна:**

```bash
python pipeline.py
```

### **2. Запуск генератора данных:**

```bash
python data_generator.py
```

**Результат:**

- Создаст 15 CSV файлов по 10,000 строк
- Сохранит в `data/input/`
- Сгенерирует файл метаданных

### **3. Мониторинг работы:**

```
============================================================
ЗАПУСК ПАЙПЛАЙНА ДЛЯ MINIO
Логи загружаются каждую минуту (если были события)
============================================================
✅ Подключение к MinIO успешно
✅ Версионирование: Enabled
Начинаю мониторинг папки: ./data/input
Обнаружен новый CSV файл: data_batch_001_20241218_143025.csv
Начинаю обработку: data_batch_001_20241218_143025.csv
✅ Файл обработан: processed-data/2024/12/18/filtered_data_batch_001_20241218_143025.csv
📊 Загружаю логи (интервал: 1 минута)
✅ Лог-файл загружен: pipeline.log
```

---

## 🏆 **Особенности реализации**

### **1. 💾 Эффективность памяти**

- 📦 Пакетное чтение CSV файлов
- 🗑️ Временные файлы удаляются после загрузки
- 📝 Логи периодически очищаются

### **2. 🛡️ Надежность**

- ✅ Проверка стабильности файлов перед обработкой
- ✅ Сохранение исходных файлов до успешной загрузки
- ✅ Фоновая загрузка логов не блокирует основной поток

### **3. 📈 Масштабируемость**

- ⚡ Асинхронная архитектура
- 🔢 Настраиваемое количество параллельных обработок
- 🐘 Поддержка больших файлов через пакетную обработку

### **4. 📊 Аудит и мониторинг**

- 📝 Детальное логирование всех операций
- 🔄 Версионирование логов в MinIO
- 📋 Метаданные о сгенерированных файлах
